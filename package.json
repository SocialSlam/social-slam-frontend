{
  "name": "social-slam",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --save node-sass --prefix frontend",
    "start": "npm start --prefix frontend"
  },
  "engines": {
    "node": ">= 12.0.0",
    "npm": ">= 6.0.0"
  },
  "dependencies": {}
}
